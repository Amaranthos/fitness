{
  "name": "fitness",
  "version": "0.1.0",
  "description": "",
  "license": "MIT",
  "homepage": "https://github.com/Amaranthos/fitness",
  "scripts": {
    "start": "concurrently \"yarn styles --watch\" \"vite\"",
    "build": "vite build",
    "styles": "npx tailwindcss -i ./src/styles.css -o ./main.css",
    "predeploy": "rm -rf dist && yarn styles && yarn build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-router-dom": "^6.2.2"
  },
  "devDependencies": {
    "babel-preset-nano-react-app": "^0.1.0",
    "concurrently": "^7.0.0",
    "gh-pages": "^3.2.3",
    "tailwindcss": "^3.0.23",
    "vite": "^2.6.7"
  },
  "babel": {
    "presets": [
      "nano-react-app"
    ]
  }
}
